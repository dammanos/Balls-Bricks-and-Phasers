{
  "name": "balls-bricks-phasers",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "zip:itch": "cd dist && zip -r ../balls-bricks-phasers-itch.zip . -x \"*.DS_Store\"",
    "package:itch": "rm -rf dist balls-bricks-phasers-itch.zip && npm run build && npm run zip:itch"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
